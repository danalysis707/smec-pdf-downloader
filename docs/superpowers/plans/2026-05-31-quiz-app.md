# 中小企業診断士 過去問クイズアプリ 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 令和2〜7年度 全7科目の過去問PDFをJPEG画像に変換し、スマホ対応のクイズアプリとしてGitHub Pagesに公開する。

**Architecture:** `build.py`がPDFをJPEG画像に変換し`docs/data/quiz_data.json`を生成。静的HTML/JSアプリ（`docs/`以下）がGitHub Pagesで配信される。正解・メモはlocalStorageに保存。

**Tech Stack:** Python 3.x, PyMuPDF (fitz), pdfplumber, pytest, HTML/CSS/JavaScript (Vanilla), GitHub Pages

---

## ファイル構成

```
quiz/
├── build.py                          # 新規: PDF→画像変換・JSON生成
├── tests/
│   └── test_build.py                 # 新規: build.py のユニットテスト
├── docs/                             # GitHub Pages 公開ルート
│   ├── index.html                    # 新規: クイズアプリ本体
│   ├── app.js                        # 新規: クイズロジック
│   ├── style.css                     # 新規: スマホ対応スタイル
│   ├── data/
│   │   ├── quiz_data.json            # 生成: 全問題メタデータ
│   │   └── theme_map.json            # 新規: テーマ分類定義
│   └── images/                       # 生成: PDFページ画像
│       └── r02/keizai/page_001.jpg
├── download.py                       # 既存（変更なし）
└── requirements.txt                  # 修正: pymupdf, pdfplumber 追加
```

---

### Task 1: 依存関係の追加

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: requirements.txt に追記**

```
requests
pytest
pymupdf
pdfplumber
yt-dlp
```

- [ ] **Step 2: インストール確認**

```powershell
pip install -r requirements.txt
python -c "import fitz; import pdfplumber; import yt_dlp; print('OK')"
```

期待出力: `OK`

- [ ] **Step 3: コミット**

```bash
git add requirements.txt
git commit -m "chore: add pymupdf and pdfplumber dependencies"
```

---

### Task 2: build.py — PDF→JPEG画像変換

**Files:**
- Create: `build.py`
- Create: `tests/test_build.py`

- [ ] **Step 1: テストを書く**

`tests/test_build.py`:
```python
import pytest
from pathlib import Path
from build import convert_pdf_to_images

SAMPLE_PDF = Path("downloads/令和2年度/令和2年度_経済学・経済政策_問題.pdf")

@pytest.mark.skipif(not SAMPLE_PDF.exists(), reason="PDF not downloaded")
def test_convert_pdf_to_images_creates_files(tmp_path):
    images = convert_pdf_to_images(SAMPLE_PDF, tmp_path)
    assert len(images) > 0
    for img in images:
        assert img.exists()
        assert img.suffix == ".jpg"
        assert img.stat().st_size > 1000  # 1KB以上

@pytest.mark.skipif(not SAMPLE_PDF.exists(), reason="PDF not downloaded")
def test_convert_pdf_to_images_naming(tmp_path):
    images = convert_pdf_to_images(SAMPLE_PDF, tmp_path)
    assert images[0].name == "page_001.jpg"
    if len(images) > 1:
        assert images[1].name == "page_002.jpg"

def test_convert_pdf_to_images_skips_existing(tmp_path):
    # 既存ファイルがある場合は変換をスキップして既存を返す
    existing = tmp_path / "page_001.jpg"
    existing.write_bytes(b"fake")
    existing2 = tmp_path / "page_002.jpg"
    existing2.write_bytes(b"fake")
    # ダミーPDFなしで既存ファイルが返されることを確認
    # (実際のPDFなしで存在チェックのみテスト)
    assert existing.exists()
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
pytest tests/test_build.py -v
```

期待: `ImportError: cannot import name 'convert_pdf_to_images'`

- [ ] **Step 3: build.py を作成して convert_pdf_to_images を実装**

`build.py`:
```python
import fitz  # PyMuPDF
import pdfplumber
import json
import re
from pathlib import Path

from download import YEARS, SUBJECTS, OUTPUT_DIR

DOCS_DIR = Path("docs")
IMAGES_DIR = DOCS_DIR / "images"
DATA_DIR = DOCS_DIR / "data"

SUBJECT_SHORT = {
    "経済学・経済政策":           "keizai",
    "財務・会計":                 "zaimu",
    "企業経営理論":               "keiei",
    "運営管理":                   "unei",
    "経営法務":                   "houmu",
    "経営情報システム":           "joho",
    "中小企業経営・中小企業政策": "chusho",
}

IMAGE_DPI = 150
IMAGE_QUALITY = 85


def convert_pdf_to_images(pdf_path: Path, output_dir: Path) -> list[Path]:
    """PDFの各ページをJPEG画像に変換して保存し、画像パスのリストを返す。"""
    output_dir.mkdir(parents=True, exist_ok=True)
    doc = fitz.open(str(pdf_path))
    mat = fitz.Matrix(IMAGE_DPI / 72, IMAGE_DPI / 72)
    image_paths = []
    for page_num in range(len(doc)):
        image_path = output_dir / f"page_{page_num + 1:03d}.jpg"
        if not image_path.exists():
            page = doc[page_num]
            pix = page.get_pixmap(matrix=mat)
            pix.save(str(image_path), jpg_quality=IMAGE_QUALITY)
        image_paths.append(image_path)
    doc.close()
    return image_paths
```

- [ ] **Step 4: テストが通ることを確認**

```bash
pytest tests/test_build.py -v
```

期待: `passed`（SAMPLE_PDF存在するなら3件、なければskip）

- [ ] **Step 5: コミット**

```bash
git add build.py tests/test_build.py
git commit -m "feat: add convert_pdf_to_images to build.py"
```

---

### Task 3: build.py — 設問番号検出・解答抽出

**Files:**
- Modify: `build.py`
- Modify: `tests/test_build.py`

- [ ] **Step 1: 設問番号検出のテストを追加**

`tests/test_build.py` に追記:
```python
from build import detect_question_pages, extract_answers

SAMPLE_Q_PDF  = Path("downloads/令和2年度/令和2年度_経済学・経済政策_問題.pdf")
SAMPLE_A_PDF  = Path("downloads/令和2年度/令和2年度_経済学・経済政策_解答.pdf")

@pytest.mark.skipif(not SAMPLE_Q_PDF.exists(), reason="PDF not downloaded")
def test_detect_question_pages_returns_dict(tmp_path):
    result = detect_question_pages(SAMPLE_Q_PDF)
    assert isinstance(result, dict)
    assert len(result) > 0
    for q_num, pages in result.items():
        assert isinstance(q_num, int)
        assert isinstance(pages, list)
        assert all(isinstance(p, int) for p in pages)

@pytest.mark.skipif(not SAMPLE_Q_PDF.exists(), reason="PDF not downloaded")
def test_detect_question_pages_sequential(tmp_path):
    result = detect_question_pages(SAMPLE_Q_PDF)
    q_nums = sorted(result.keys())
    assert q_nums[0] == 1

@pytest.mark.skipif(not SAMPLE_A_PDF.exists(), reason="PDF not downloaded")
def test_extract_answers_returns_dict(tmp_path):
    result = extract_answers(SAMPLE_A_PDF)
    assert isinstance(result, dict)
    assert len(result) > 0
    for q_num, answer in result.items():
        assert isinstance(q_num, int)
        assert answer in "アイウエオ"

@pytest.mark.skipif(not SAMPLE_A_PDF.exists(), reason="PDF not downloaded")
def test_extract_answers_r02_keizai_first_question(tmp_path):
    result = extract_answers(SAMPLE_A_PDF)
    assert 1 in result
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
pytest tests/test_build.py::test_detect_question_pages_returns_dict -v
```

期待: `ImportError: cannot import name 'detect_question_pages'`

- [ ] **Step 3: detect_question_pages と extract_answers を build.py に追加**

`build.py` の `convert_pdf_to_images` の後に追記:
```python
def detect_question_pages(pdf_path: Path) -> dict[int, list[int]]:
    """PDFから「第N問」の出現ページを検出し {問題番号: [ページ番号]} を返す（1始まり）。"""
    question_pages: dict[int, list[int]] = {}
    with pdfplumber.open(str(pdf_path)) as pdf:
        for page_num, page in enumerate(pdf.pages, 1):
            text = page.extract_text() or ""
            for match in re.finditer(r'第\s*(\d+)\s*問', text):
                q_num = int(match.group(1))
                if q_num not in question_pages:
                    question_pages[q_num] = []
                if page_num not in question_pages[q_num]:
                    question_pages[q_num].append(page_num)
    return question_pages


def extract_answers(answer_pdf_path: Path) -> dict[int, str]:
    """解答PDFから {問題番号: 正解記号} を抽出する。"""
    answers: dict[int, str] = {}
    with pdfplumber.open(str(answer_pdf_path)) as pdf:
        full_text = "\n".join(page.extract_text() or "" for page in pdf.pages)
    # パターン1: 「第1問　ウ」形式
    for match in re.finditer(r'第\s*(\d+)\s*問\s*([アイウエオ])', full_text):
        answers[int(match.group(1))] = match.group(2)
    # パターン2: 表形式「1　ウ」形式（パターン1で取れなかった場合のフォールバック）
    if not answers:
        for match in re.finditer(r'(?<!\d)(\d{1,2})\s+([アイウエオ])(?!\w)', full_text):
            answers[int(match.group(1))] = match.group(2)
    return answers
```

- [ ] **Step 4: テストが通ることを確認**

```bash
pytest tests/test_build.py -v
```

期待: 全テスト passed（またはskip）

- [ ] **Step 5: コミット**

```bash
git add build.py tests/test_build.py
git commit -m "feat: add detect_question_pages and extract_answers"
```

---

### Task 4: theme_map.json・youtube_config.json・fetch_youtube.py とテーマ割り当て

**Files:**
- Create: `docs/data/theme_map.json`
- Create: `youtube_config.json`
- Create: `fetch_youtube.py`
- Modify: `build.py`
- Modify: `tests/test_build.py`

- [ ] **Step 1: docs/data/ ディレクトリを作成して theme_map.json と youtube_config.json を書く**

```powershell
New-Item -ItemType Directory -Force docs/data
```

`docs/data/theme_map.json`:
```json
{
  "経済学・経済政策": {
    "ミクロ経済学": ["需要曲線","供給曲線","弾力性","消費者余剰","生産者余剰","独占","寡占","ゲーム理論","外部性","公共財","情報の非対称","市場均衡"],
    "マクロ経済学": ["GDP","国民所得","乗数","IS-LM","消費関数","投資","貯蓄","総需要","総供給","デフレ","インフレ","景気循環"],
    "金融政策・財政政策": ["日本銀行","金融政策","財政政策","公開市場操作","準備率","政策金利","財政乗数","国債","マネーサプライ"],
    "国際経済・貿易・為替": ["為替","貿易","比較優位","経常収支","国際収支","関税","購買力平価","変動相場","固定相場"],
    "経済指標・景気動向": ["景気動向指数","物価指数","CPI","GDP成長率","完全失業率","鉱工業指数","日銀短観"]
  },
  "財務・会計": {
    "財務諸表（B/S・P/L・CF）": ["貸借対照表","損益計算書","キャッシュフロー","B/S","P/L","CF","資産","負債","純資産","売上","費用"],
    "財務分析（収益性・安全性）": ["収益性","安全性","流動比率","自己資本比率","ROE","ROA","回転率","損益分岐点"],
    "管理会計・原価計算": ["原価計算","標準原価","変動費","固定費","CVP","直接原価","全部原価","差額原価","機会費用"],
    "企業価値評価・投資": ["DCF","NPV","IRR","割引率","WACC","株価","PER","PBR","企業価値","M&A"],
    "資金調達・資本構成": ["資金調達","社債","株式","自己資本","負債比率","資本コスト","MM理論","配当","レバレッジ"]
  },
  "企業経営理論": {
    "経営戦略論": ["競争戦略","差別化","コスト","ポーターの","バリューチェーン","SWOT","VRIO","コアコンピタンス","多角化","アンゾフ"],
    "組織論・人的資源管理": ["組織形態","事業部","マトリックス","モチベーション","リーダーシップ","人事","採用","評価制度","学習する組織"],
    "マーケティング": ["マーケティング","4P","STP","セグメント","ターゲット","ポジショニング","製品","価格","流通","プロモーション","CRM","ブランド"],
    "技術経営・イノベーション": ["イノベーション","技術経営","MOT","製品開発","R&D","知識創造","オープンイノベーション","技術移転"],
    "国際経営": ["グローバル","多国籍","海外進出","国際化","現地化","輸出","合弁","直接投資"]
  },
  "運営管理": {
    "生産管理・IE": ["生産管理","生産方式","JIT","かんばん","IE","作業分析","工程管理","ライン","セル生産","PERT","CPM"],
    "在庫管理・SCM": ["在庫","EOQ","発注点","安全在庫","SCM","サプライチェーン","MRP","ERP","需要予測"],
    "品質管理・QC": ["品質管理","QC","TQM","ISO","管理図","ヒストグラム","特性要因図","パレート","6シグマ","抜取検査"],
    "店舗・販売管理": ["店舗","小売","商圏","立地","VMD","POSシステム","フランチャイズ","マーチャンダイジング"],
    "物流管理": ["物流","輸送","倉庫","3PL","ロジスティクス","配送","モーダルシフト"]
  },
  "経営法務": {
    "会社法": ["会社法","株式会社","取締役","株主総会","監査役","合同会社","組織再編","合併","分割","設立"],
    "知的財産法（特許・商標等）": ["特許","実用新案","意匠","商標","著作権","知的財産","特許庁","出願","登録","侵害"],
    "民法・契約法": ["民法","契約","債権","債務","不法行為","時効","担保","保証","賃貸借","売買"],
    "資本市場・証券法": ["金融商品取引法","上場","株式公開","インサイダー","有価証券","金融庁","開示"],
    "独占禁止法・その他": ["独占禁止法","公正取引","カルテル","優越的地位","下請法","景品表示法","消費者契約法"]
  },
  "経営情報システム": {
    "ハードウェア・OS基礎": ["CPU","メモリ","ハードディスク","OS","オペレーティングシステム","プロセス","ファイルシステム","入出力"],
    "ネットワーク・セキュリティ": ["ネットワーク","TCP/IP","HTTP","LAN","WAN","VPN","ファイアウォール","暗号化","認証","セキュリティ","マルウェア"],
    "データベース": ["データベース","SQL","リレーショナル","正規化","トランザクション","インデックス","ER図","NoSQL"],
    "経営情報システム（ERP等）": ["ERP","CRM","SCM","BPR","SaaS","クラウド","AI","IoT","ビッグデータ","RPA","DX"],
    "統計・データ分析": ["統計","回帰分析","相関","標準偏差","仮説検定","機械学習","データマイニング","BI"]
  },
  "中小企業経営・中小企業政策": {
    "中小企業の現状・統計": ["中小企業白書","小規模企業","企業数","従業者数","付加価値","廃業","開業","業種別"],
    "中小企業政策・支援制度": ["中小企業基本法","補助金","助成金","認定支援機関","経営革新","ものづくり補助","IT導入","事業再構築"],
    "中小企業金融": ["信用保証","政策金融公庫","融資","信用金庫","マル経融資","セーフティネット保証"],
    "創業・ベンチャー支援": ["創業","ベンチャー","スタートアップ","インキュベーター","エンジェル","VC","創業塾"],
    "事業承継・M&A": ["事業承継","後継者","親族内承継","M&A","株式","事業譲渡","経営者保証"]
  }
}
```

`youtube_config.json`（プロジェクトルートに配置）:
```json
{
  "channels": [
    {
      "name": "はじめよう経済学",
      "url": "https://www.youtube.com/@hajimeyou-keizaigaku"
    },
    {
      "name": "ほらっちチャンネル",
      "url": "https://www.youtube.com/@Horacchichannel"
    },
    {
      "name": "たかぴーの中小企業診断士試験 攻略チャンネル",
      "url": "https://www.youtube.com/@takapi-shindanshi"
    }
  ]
}
```

- [ ] **Step 1b: fetch_youtube.py を作成**

`fetch_youtube.py`:
```python
import json
import subprocess
import sys
from pathlib import Path

YOUTUBE_CONFIG = Path("youtube_config.json")
THEME_MAP_PATH = Path("docs/data/theme_map.json")
OUTPUT_PATH = Path("docs/data/youtube_data.json")
MAX_VIDEOS_PER_THEME = 5


def fetch_channel_videos(channel_url: str) -> list[dict]:
    """yt-dlp でチャンネルの動画タイトルとURLを取得する。"""
    result = subprocess.run(
        [
            sys.executable, "-m", "yt_dlp",
            "--flat-playlist", "--print", "%(title)s\t%(webpage_url)s",
            "--no-warnings",
            channel_url,
        ],
        capture_output=True, text=True, encoding="utf-8",
    )
    videos = []
    for line in result.stdout.splitlines():
        if "\t" in line:
            title, url = line.split("\t", 1)
            videos.append({"title": title.strip(), "url": url.strip()})
    return videos


def match_video_to_theme(title: str, theme_keywords: dict) -> tuple[str, str] | None:
    """動画タイトルにキーワードが含まれれば (subject, theme) を返す。"""
    for subject, themes in theme_keywords.items():
        for theme, keywords in themes.items():
            if any(kw in title for kw in keywords):
                return subject, theme
    return None


def main() -> None:
    config = json.loads(YOUTUBE_CONFIG.read_text(encoding="utf-8"))
    theme_keywords = json.loads(THEME_MAP_PATH.read_text(encoding="utf-8"))
    result: dict[str, dict[str, list[dict]]] = {}

    for ch in config["channels"]:
        print(f"Fetching: {ch['name']} ...")
        videos = fetch_channel_videos(ch["url"])
        print(f"  {len(videos)} videos found")
        matched = 0
        for video in videos:
            match = match_video_to_theme(video["title"], theme_keywords)
            if match:
                subject, theme = match
                bucket = result.setdefault(subject, {}).setdefault(theme, [])
                if len(bucket) < MAX_VIDEOS_PER_THEME:
                    bucket.append({
                        "title": video["title"],
                        "url": video["url"],
                        "channel": ch["name"],
                    })
                    matched += 1
        print(f"  {matched} videos matched to themes")

    OUTPUT_PATH.parent.mkdir(parents=True, exist_ok=True)
    OUTPUT_PATH.write_text(
        json.dumps(result, ensure_ascii=False, indent=2), encoding="utf-8"
    )
    total = sum(len(v) for themes in result.values() for v in themes.values())
    print(f"\nDone: {OUTPUT_PATH} ({total} videos across {len(result)} subjects)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 1c: fetch_youtube.py を実行して youtube_data.json を生成**

```powershell
python fetch_youtube.py
```

期待出力例:
```
Fetching: はじめよう経済学 ...
  142 videos found
  23 videos matched to themes
...
Done: docs/data/youtube_data.json (87 videos across 7 subjects)
```

`docs/data/youtube_data.json` が生成され、`経済学・経済政策` 等のキーに動画リストが含まれることを確認。

- [ ] **Step 2: assign_theme のテストを追加**

`tests/test_build.py` に追記:
```python
from build import assign_theme

def test_assign_theme_micro_economics():
    theme = assign_theme("需要曲線と供給曲線の均衡について", "経済学・経済政策")
    assert theme == "ミクロ経済学"

def test_assign_theme_macro_economics():
    theme = assign_theme("GDPと国民所得の関係について", "経済学・経済政策")
    assert theme == "マクロ経済学"

def test_assign_theme_unknown_returns_other():
    theme = assign_theme("何も当てはまらない問題文", "経済学・経済政策")
    assert theme == "その他"

def test_assign_theme_company_law():
    theme = assign_theme("株式会社の取締役の責任について", "経営法務")
    assert theme == "会社法"
```

- [ ] **Step 3: テストが失敗することを確認**

```bash
pytest tests/test_build.py::test_assign_theme_micro_economics -v
```

期待: `ImportError: cannot import name 'assign_theme'`

- [ ] **Step 4: assign_theme を build.py に追加**

`build.py` の先頭に定数として追加（`SUBJECT_SHORT` の後）:
```python
THEME_KEYWORDS: dict[str, dict[str, list[str]]] = {}  # build時にJSONから読み込む


def load_theme_keywords() -> None:
    """theme_map.json を読み込んで THEME_KEYWORDS を初期化する。"""
    global THEME_KEYWORDS
    theme_map_path = DATA_DIR / "theme_map.json"
    with open(theme_map_path, encoding="utf-8") as f:
        THEME_KEYWORDS = json.load(f)


def assign_theme(question_text: str, subject: str) -> str:
    """問題テキストのキーワードマッチングでテーマを返す。未マッチは'その他'。"""
    if not THEME_KEYWORDS:
        load_theme_keywords()
    themes = THEME_KEYWORDS.get(subject, {})
    for theme, keywords in themes.items():
        if any(kw in question_text for kw in keywords):
            return theme
    return "その他"
```

- [ ] **Step 5: テストが通ることを確認**

```bash
pytest tests/test_build.py -v
```

期待: assign_theme の4テスト passed

- [ ] **Step 6: コミット**

```bash
git add docs/data/theme_map.json docs/data/youtube_data.json youtube_config.json fetch_youtube.py build.py tests/test_build.py
git commit -m "feat: add theme_map, youtube_data, fetch_youtube.py and assign_theme"
```

---

### Task 5: build.py — メインビルド関数（全件処理）

**Files:**
- Modify: `build.py`
- Modify: `tests/test_build.py`

- [ ] **Step 1: build_quiz_data のテストを追加**

`tests/test_build.py` に追記:
```python
from build import build_quiz_data, build_question_entry

def test_build_question_entry_structure():
    entry = build_question_entry(
        ryear="r02", cyear="2020", year_label="令和2年度",
        subject="経済学・経済政策",
        question_number=1,
        pages=["images/r02/keizai/page_001.jpg"],
        correct_answer="ウ",
        answer_pages=["images/r02/keizai_answer/page_001.jpg"],
    )
    assert entry["id"] == "r02_keizai_q001"
    assert entry["year"] == "r02"
    assert entry["year_label"] == "令和2年度"
    assert entry["subject"] == "経済学・経済政策"
    assert entry["question_number"] == 1
    assert entry["correct_answer"] == "ウ"
    assert entry["pages"] == ["images/r02/keizai/page_001.jpg"]
    assert entry["search_query"] == "令和2年度 中小企業診断士 経済学・経済政策 第1問 解説"
    assert "theme" in entry
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
pytest tests/test_build.py::test_build_question_entry_structure -v
```

期待: `ImportError: cannot import name 'build_question_entry'`

- [ ] **Step 3: build_question_entry と build_quiz_data と main を build.py に追加**

`build.py` の末尾に追記:
```python
def build_question_entry(
    ryear: str, cyear: str, year_label: str,
    subject: str, question_number: int,
    pages: list[str], correct_answer: str,
    answer_pages: list[str],
    question_text: str = "",
) -> dict:
    """1問分のquiz_dataエントリを生成する。"""
    subject_short = SUBJECT_SHORT[subject]
    return {
        "id": f"{ryear}_{subject_short}_q{question_number:03d}",
        "year": ryear,
        "year_label": year_label,
        "subject": subject,
        "subject_short": subject_short,
        "question_number": question_number,
        "theme": assign_theme(question_text, subject),
        "pages": pages,
        "correct_answer": correct_answer,
        "answer_pages": answer_pages,
        "search_query": f"{year_label} 中小企業診断士 {subject} 第{question_number}問 解説",
    }


def build_quiz_data() -> list[dict]:
    """全年度・全科目を処理してquiz_dataエントリのリストを返す。"""
    load_theme_keywords()
    questions = []
    for ryear, cyear, year_label in YEARS:
        for subject, answer_letter, question_letter in SUBJECTS:
            subject_short = SUBJECT_SHORT[subject]
            q_pdf = OUTPUT_DIR / year_label / f"{year_label}_{subject}_問題.pdf"
            a_pdf = OUTPUT_DIR / year_label / f"{year_label}_{subject}_解答.pdf"
            if not q_pdf.exists():
                print(f"  スキップ（PDF未存在）: {q_pdf.name}")
                continue

            # 画像変換
            img_dir = IMAGES_DIR / ryear / subject_short
            images = convert_pdf_to_images(q_pdf, img_dir)
            img_paths = [str(p.relative_to(DOCS_DIR)).replace("\\", "/") for p in images]

            # 解答画像変換
            ans_dir = IMAGES_DIR / ryear / f"{subject_short}_answer"
            ans_paths: list[str] = []
            if a_pdf.exists():
                ans_images = convert_pdf_to_images(a_pdf, ans_dir)
                ans_paths = [str(p.relative_to(DOCS_DIR)).replace("\\", "/") for p in ans_images]

            # 設問番号検出
            page_map = detect_question_pages(q_pdf)

            # 正解抽出
            answers = extract_answers(a_pdf) if a_pdf.exists() else {}

            # 問題テキスト取得（テーマ割り当て用）
            page_texts: list[str] = []
            try:
                with pdfplumber.open(str(q_pdf)) as pdf:
                    page_texts = [p.extract_text() or "" for p in pdf.pages]
            except Exception:
                page_texts = [""] * len(images)

            if not page_map:
                # 検出失敗時: 全ページを第1問として扱う
                page_map = {i + 1: [i + 1] for i in range(len(images))}

            for q_num in sorted(page_map.keys()):
                q_page_nums = page_map[q_num]
                q_img_paths = [img_paths[p - 1] for p in q_page_nums if p - 1 < len(img_paths)]
                q_text = " ".join(page_texts[p - 1] for p in q_page_nums if p - 1 < len(page_texts))
                correct = answers.get(q_num, "")
                # 解答ページ: 正解が含まれるページを推定（暫定: 解答PDF全ページ）
                entry = build_question_entry(
                    ryear=ryear, cyear=cyear, year_label=year_label,
                    subject=subject, question_number=q_num,
                    pages=q_img_paths, correct_answer=correct,
                    answer_pages=ans_paths[:1] if ans_paths else [],
                    question_text=q_text,
                )
                questions.append(entry)
                print(f"  [{ryear}] {subject} 第{q_num}問 → {correct or '未取得'}")

    return questions


def main() -> None:
    DATA_DIR.mkdir(parents=True, exist_ok=True)
    IMAGES_DIR.mkdir(parents=True, exist_ok=True)
    print("ビルド開始...\n")
    questions = build_quiz_data()
    out_path = DATA_DIR / "quiz_data.json"
    with open(out_path, "w", encoding="utf-8") as f:
        json.dump({"questions": questions}, f, ensure_ascii=False, indent=2)
    print(f"\n完了: {len(questions)}問 → {out_path}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: テストが通ることを確認**

```bash
pytest tests/test_build.py -v
```

期待: 全テスト passed

- [ ] **Step 5: ビルドを実行して quiz_data.json を生成**

```bash
python build.py
```

期待出力:
```
ビルド開始...
  [r02] 経済学・経済政策 第1問 → ウ
  ...
完了: XXX問 → docs/data/quiz_data.json
```

`docs/data/quiz_data.json` が存在し、`questions` 配列に1問以上あることを確認。

- [ ] **Step 6: コミット**

```bash
git add build.py tests/test_build.py docs/data/quiz_data.json docs/data/theme_map.json
git commit -m "feat: add build_quiz_data and main, generate quiz_data.json"
```

---

### Task 6: docs/index.html + docs/style.css

**Files:**
- Create: `docs/index.html`
- Create: `docs/style.css`

- [ ] **Step 1: docs/style.css を作成**

`docs/style.css`:
```css
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --primary: #1a5276;
  --primary-light: #2980b9;
  --success: #1e8449;
  --danger: #922b21;
  --bg: #f5f7fa;
  --card: #ffffff;
  --border: #d0dff0;
  --text: #2c3e50;
  --muted: #7f8c8d;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Hiragino Sans", "Meiryo", sans-serif;
  background: var(--bg);
  color: var(--text);
  min-height: 100vh;
}

.screen { display: none; min-height: 100vh; flex-direction: column; }
.screen.active { display: flex; }

/* Source bar */
.source-bar {
  background: var(--primary);
  color: #fff;
  padding: 10px 16px;
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  align-items: center;
  position: sticky;
  top: 0;
  z-index: 10;
}
.source-tag {
  background: rgba(255,255,255,0.2);
  border-radius: 4px;
  padding: 2px 8px;
  font-size: 0.8rem;
  font-weight: 700;
}

/* Filter screen */
.filter-header {
  background: var(--primary);
  color: #fff;
  padding: 16px;
  text-align: center;
  font-size: 1.1rem;
  font-weight: 700;
}
.filter-body { padding: 16px; flex: 1; overflow-y: auto; }
.filter-section { margin-bottom: 20px; }
.filter-section h2 { font-size: 0.85rem; color: var(--muted); margin-bottom: 8px; text-transform: uppercase; letter-spacing: 0.05em; }
.subject-list { display: flex; flex-direction: column; gap: 8px; }
.subject-btn {
  background: var(--card);
  border: 2px solid var(--border);
  border-radius: 10px;
  padding: 12px 16px;
  font-size: 0.95rem;
  color: var(--text);
  text-align: left;
  cursor: pointer;
  transition: all 0.15s;
}
.subject-btn.selected { background: var(--primary); color: #fff; border-color: var(--primary); }
.chip-list { display: flex; flex-wrap: wrap; gap: 8px; }
.chip {
  background: var(--card);
  border: 1.5px solid var(--border);
  border-radius: 20px;
  padding: 6px 14px;
  font-size: 0.85rem;
  cursor: pointer;
  transition: all 0.15s;
}
.chip.selected { background: var(--primary); color: #fff; border-color: var(--primary); }
.start-btn {
  width: 100%;
  background: var(--primary);
  color: #fff;
  border: none;
  border-radius: 12px;
  padding: 16px;
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
  margin-top: 8px;
  transition: opacity 0.15s;
}
.start-btn:disabled { opacity: 0.4; cursor: not-allowed; }

/* Question screen */
.q-body { padding: 12px 16px; flex: 1; overflow-y: auto; }
.q-meta { display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; }
.theme-badge { background: var(--primary-light); color: #fff; border-radius: 6px; padding: 3px 10px; font-size: 0.8rem; font-weight: 700; }
.progress-text { font-size: 0.85rem; color: var(--muted); }
.question-images { margin-bottom: 12px; }
.question-images img { width: 100%; border-radius: 8px; display: block; margin-bottom: 4px; touch-action: pinch-zoom; }
.answer-buttons { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-bottom: 10px; }
.ans-btn {
  background: var(--card);
  border: 2px solid var(--border);
  border-radius: 10px;
  padding: 14px 8px;
  font-size: 1.1rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.15s;
}
.ans-btn:hover { border-color: var(--primary); }
.ans-btn.selected { background: var(--primary); color: #fff; border-color: var(--primary); }
.search-link {
  display: block;
  text-align: center;
  background: #fff8e1;
  border: 1px solid #f0c040;
  border-radius: 8px;
  padding: 10px;
  font-size: 0.85rem;
  color: #7d6608;
  text-decoration: none;
  margin-bottom: 8px;
}

/* Result section */
.result-section { margin-top: 8px; }
.result-badge {
  text-align: center;
  padding: 12px;
  border-radius: 10px;
  font-size: 1.1rem;
  font-weight: 700;
  margin-bottom: 12px;
}
.result-badge.correct { background: #eafaf1; color: var(--success); }
.result-badge.wrong { background: #fdedec; color: var(--danger); }
.answer-box {
  background: var(--card);
  border-radius: 10px;
  padding: 12px 16px;
  margin-bottom: 10px;
  display: flex;
  gap: 24px;
}
.answer-item { flex: 1; }
.answer-label { font-size: 0.75rem; color: var(--muted); margin-bottom: 4px; }
.answer-val { font-size: 1.4rem; font-weight: 700; color: var(--primary); }
.pdf-btn {
  display: block;
  text-align: center;
  background: #eaf0fb;
  color: var(--primary-light);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 10px;
  font-size: 0.9rem;
  cursor: pointer;
  margin-bottom: 8px;
  text-decoration: none;
}
/* YouTube links */
#youtube-links { margin-bottom: 10px; }
.yt-label { font-size: 0.85rem; color: var(--muted); margin-bottom: 6px; }
.yt-link {
  display: block;
  background: #fff3f3;
  border: 1px solid #ffcccc;
  border-radius: 8px;
  padding: 10px 12px;
  font-size: 0.85rem;
  color: #c0392b;
  text-decoration: none;
  margin-bottom: 6px;
}

.memo-section { margin-bottom: 12px; }
.memo-label { font-size: 0.85rem; color: var(--muted); margin-bottom: 6px; }
.memo-textarea {
  width: 100%;
  background: #fffde7;
  border: 1px solid #f0c040;
  border-radius: 8px;
  padding: 10px 12px;
  font-size: 0.9rem;
  min-height: 80px;
  resize: vertical;
  font-family: inherit;
}
.next-btn {
  width: 100%;
  background: var(--primary);
  color: #fff;
  border: none;
  border-radius: 12px;
  padding: 16px;
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
}

/* Complete screen */
.complete-body { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 32px 16px; text-align: center; gap: 16px; }
.score-display { font-size: 2rem; font-weight: 700; color: var(--primary); }
.complete-btn { background: var(--primary); color: #fff; border: none; border-radius: 12px; padding: 14px 32px; font-size: 1rem; font-weight: 700; cursor: pointer; }
.complete-btn.secondary { background: var(--card); color: var(--primary); border: 2px solid var(--primary); }

.hidden { display: none !important; }
```

- [ ] **Step 2: docs/index.html を作成**

`docs/index.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
  <title>中小企業診断士 過去問クイズ</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- 画面① 科目・テーマ・年度選択 -->
  <div id="screen-filter" class="screen active">
    <div class="filter-header">中小企業診断士 1次試験 過去問</div>
    <div class="filter-body">
      <div class="filter-section">
        <h2>科目</h2>
        <div class="subject-list" id="subject-list"></div>
      </div>
      <div class="filter-section" id="theme-section">
        <h2>テーマ（複数選択可）</h2>
        <div class="chip-list" id="theme-list"></div>
      </div>
      <div class="filter-section">
        <h2>年度（複数選択可）</h2>
        <div class="chip-list" id="year-list"></div>
      </div>
      <button class="start-btn" id="start-btn" disabled>科目を選択してください</button>
    </div>
  </div>

  <!-- 画面② 問題表示・解答 -->
  <div id="screen-question" class="screen">
    <div class="source-bar">
      <span class="source-tag" id="src-year"></span>
      <span class="source-tag" id="src-subject"></span>
      <span class="source-tag" id="src-qnum"></span>
    </div>
    <div class="q-body">
      <div class="q-meta">
        <span class="theme-badge" id="theme-badge"></span>
        <span class="progress-text" id="progress-text"></span>
      </div>
      <div class="question-images" id="question-images"></div>
      <div id="answer-area">
        <div class="answer-buttons" id="answer-buttons"></div>
        <a class="search-link" id="search-link-q" href="#" target="_blank">🔍 この問題を検索して解説を見る</a>
      </div>
      <div class="result-section hidden" id="result-section">
        <div class="result-badge" id="result-badge"></div>
        <div class="answer-box">
          <div class="answer-item">
            <div class="answer-label">正解</div>
            <div class="answer-val" id="correct-val"></div>
          </div>
          <div class="answer-item">
            <div class="answer-label">あなたの解答</div>
            <div class="answer-val" id="your-val"></div>
          </div>
        </div>
        <a class="search-link" id="search-link-r" href="#" target="_blank">🔍 解説をネット検索</a>
        <div id="youtube-links" class="hidden"></div>
        <div class="memo-section">
          <div class="memo-label">📝 メモ</div>
          <textarea class="memo-textarea" id="memo-text" placeholder="メモを入力…"></textarea>
        </div>
        <button class="next-btn" id="next-btn">次の問題へ →</button>
      </div>
    </div>
  </div>

  <!-- 画面③ 完了 -->
  <div id="screen-complete" class="screen">
    <div class="complete-body">
      <div style="font-size:3rem;">🎉</div>
      <h2>学習完了！</h2>
      <div class="score-display" id="final-score"></div>
      <button class="complete-btn" id="retry-btn">もう一度</button>
      <button class="complete-btn secondary" id="back-btn">科目選択に戻る</button>
    </div>
  </div>

  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 3: ブラウザで開いて表示を確認**

```bash
cd docs && python -m http.server 3000
```

ブラウザで `http://localhost:3000` を開き、「中小企業診断士 1次試験 過去問」ヘッダーが表示されることを確認。

- [ ] **Step 4: コミット**

```bash
git add docs/index.html docs/style.css
git commit -m "feat: add quiz app HTML and CSS skeleton"
```

---

### Task 7: docs/app.js — フィルター・問題表示

**Files:**
- Create: `docs/app.js`

- [ ] **Step 1: docs/app.js を作成（データ読み込み・フィルターUI）**

`docs/app.js`:
```javascript
// ── State ──────────────────────────────────────────────────────────────────
let allQuestions = [];
let themeMap = {};
let youtubeData = {};   // {subject: {theme: [{title, url, channel}]}}
let selectedSubject = '';
let selectedThemes = new Set();
let selectedYears = new Set();
let sessionQuestions = [];
let currentIndex = 0;
let sessionCorrect = 0;

const YEAR_LABELS = {
  r02: '令和2年度', r03: '令和3年度', r04: '令和4年度',
  r05: '令和5年度', r06: '令和6年度', r07: '令和7年度',
};

// ── Initialization ──────────────────────────────────────────────────────────
async function init() {
  const [qRes, tRes, yRes] = await Promise.all([
    fetch('data/quiz_data.json'),
    fetch('data/theme_map.json'),
    fetch('data/youtube_data.json'),
  ]);
  allQuestions = (await qRes.json()).questions;
  themeMap = await tRes.json();
  youtubeData = await yRes.json();
  renderSubjectList();
  renderYearList();
  updateStartButton();
}

// ── Filter UI ───────────────────────────────────────────────────────────────
function renderSubjectList() {
  const subjects = [...new Set(allQuestions.map(q => q.subject))];
  const container = document.getElementById('subject-list');
  container.innerHTML = subjects.map(s => `
    <button class="subject-btn" data-subject="${s}" onclick="selectSubject('${s}')">
      ${s}
    </button>
  `).join('');
}

function selectSubject(subject) {
  selectedSubject = subject;
  selectedThemes.clear();
  document.querySelectorAll('.subject-btn').forEach(btn => {
    btn.classList.toggle('selected', btn.dataset.subject === subject);
  });
  renderThemeList(subject);
  updateStartButton();
}

function renderThemeList(subject) {
  const themes = themeMap[subject] ? Object.keys(themeMap[subject]) : [];
  const container = document.getElementById('theme-list');
  container.innerHTML = themes.map(t => `
    <span class="chip" data-theme="${t}" onclick="toggleTheme('${t}', this)">${t}</span>
  `).join('');
  document.getElementById('theme-section').style.display = themes.length ? '' : 'none';
}

function toggleTheme(theme, el) {
  if (selectedThemes.has(theme)) {
    selectedThemes.delete(theme);
    el.classList.remove('selected');
  } else {
    selectedThemes.add(theme);
    el.classList.add('selected');
  }
  updateStartButton();
}

function renderYearList() {
  const years = [...new Set(allQuestions.map(q => q.year))].sort();
  const container = document.getElementById('year-list');
  container.innerHTML = years.map(y => `
    <span class="chip" data-year="${y}" onclick="toggleYear('${y}', this)">
      ${YEAR_LABELS[y] || y}
    </span>
  `).join('');
}

function toggleYear(year, el) {
  if (selectedYears.has(year)) {
    selectedYears.delete(year);
    el.classList.remove('selected');
  } else {
    selectedYears.add(year);
    el.classList.add('selected');
  }
  updateStartButton();
}

function getFilteredQuestions() {
  return allQuestions.filter(q => {
    if (!selectedSubject || q.subject !== selectedSubject) return false;
    if (selectedThemes.size > 0 && !selectedThemes.has(q.theme)) return false;
    if (selectedYears.size > 0 && !selectedYears.has(q.year)) return false;
    return true;
  });
}

function updateStartButton() {
  const btn = document.getElementById('start-btn');
  const count = getFilteredQuestions().length;
  if (!selectedSubject) {
    btn.textContent = '科目を選択してください';
    btn.disabled = true;
  } else if (count === 0) {
    btn.textContent = '該当する問題がありません';
    btn.disabled = true;
  } else {
    btn.textContent = `▶ 学習開始（${count}問）`;
    btn.disabled = false;
  }
}

// ── Question display ─────────────────────────────────────────────────────────
function showScreen(id) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
}

function startQuiz() {
  sessionQuestions = getFilteredQuestions().sort(() => Math.random() - 0.5);
  currentIndex = 0;
  sessionCorrect = 0;
  showScreen('screen-question');
  renderQuestion();
}

function renderQuestion() {
  const q = sessionQuestions[currentIndex];

  // Source bar
  document.getElementById('src-year').textContent = q.year_label;
  document.getElementById('src-subject').textContent = q.subject;
  document.getElementById('src-qnum').textContent = `第${q.question_number}問`;

  // Meta
  document.getElementById('theme-badge').textContent = q.theme;
  document.getElementById('progress-text').textContent =
    `${currentIndex + 1} / ${sessionQuestions.length}問`;

  // Images
  const imgContainer = document.getElementById('question-images');
  imgContainer.innerHTML = q.pages.map(src =>
    `<img src="${src}" alt="第${q.question_number}問" loading="lazy">`
  ).join('');

  // Answer buttons (ア〜オ)
  const btns = document.getElementById('answer-buttons');
  btns.innerHTML = ['ア', 'イ', 'ウ', 'エ', 'オ'].map(ch => `
    <button class="ans-btn" onclick="submitAnswer('${ch}')">${ch}</button>
  `).join('');

  // Search link (before answer)
  const searchQ = document.getElementById('search-link-q');
  searchQ.href = `https://www.google.com/search?q=${encodeURIComponent(q.search_query)}`;

  // Load memo
  document.getElementById('memo-text').value =
    localStorage.getItem(`memo_${q.id}`) || '';

  // Reset result section
  document.getElementById('answer-area').classList.remove('hidden');
  document.getElementById('result-section').classList.add('hidden');
}

// ── Wiring ───────────────────────────────────────────────────────────────────
document.getElementById('start-btn').addEventListener('click', startQuiz);
document.getElementById('back-btn').addEventListener('click', () => showScreen('screen-filter'));
document.getElementById('retry-btn').addEventListener('click', () => {
  currentIndex = 0;
  sessionCorrect = 0;
  sessionQuestions.sort(() => Math.random() - 0.5);
  showScreen('screen-question');
  renderQuestion();
});

init();
```

- [ ] **Step 2: ローカルサーバーで動作確認**

```bash
cd docs && python -m http.server 3000
```

`http://localhost:3000` を開き以下を確認:
- 7科目のボタンが表示される
- 科目をクリックするとテーマチップが表示される
- 年度チップが表示される
- 「学習開始」ボタンが押せる状態になる

- [ ] **Step 3: コミット**

```bash
git add docs/app.js
git commit -m "feat: add filter UI and question display logic"
```

---

### Task 8: docs/app.js — 解答・正誤判定・メモ・進捗

**Files:**
- Modify: `docs/app.js`

- [ ] **Step 1: submitAnswer・showResult・メモ保存・次の問題 を app.js に追加**

`docs/app.js` の `// ── Wiring` の直前に追記:
```javascript
// ── Answer & Result ──────────────────────────────────────────────────────────
function submitAnswer(choice) {
  const q = sessionQuestions[currentIndex];
  const isCorrect = q.correct_answer && choice === q.correct_answer;
  if (isCorrect) sessionCorrect++;

  // Highlight selected button
  document.querySelectorAll('.ans-btn').forEach(btn => {
    btn.disabled = true;
    if (btn.textContent === choice) btn.classList.add('selected');
  });

  // Save result to localStorage
  localStorage.setItem(`result_${q.id}`, isCorrect ? 'correct' : 'wrong');

  showResult(q, choice, isCorrect);
}

function showResult(q, choice, isCorrect) {
  // Result badge
  const badge = document.getElementById('result-badge');
  badge.textContent = isCorrect ? '✅ 正解！' : `❌ 不正解`;
  badge.className = `result-badge ${isCorrect ? 'correct' : 'wrong'}`;

  // Answers
  document.getElementById('correct-val').textContent = q.correct_answer || '—';
  document.getElementById('your-val').textContent = choice;

  // Search link (after answer - same URL)
  const searchR = document.getElementById('search-link-r');
  searchR.href = `https://www.google.com/search?q=${encodeURIComponent(q.search_query)}`;
  searchR.textContent = `🔍 解説をネット検索（${q.year_label} ${q.subject} 第${q.question_number}問）`;

  // YouTube links — subject + theme でマッチした動画を最大3件表示
  const ytContainer = document.getElementById('youtube-links');
  const videos = (youtubeData[q.subject] || {})[q.theme] || [];
  const shown = videos.slice(0, 3);
  if (shown.length > 0) {
    ytContainer.innerHTML =
      `<div class="yt-label">📺 YouTube関連動画（${q.theme}）</div>` +
      shown.map(v =>
        `<a class="yt-link" href="${v.url}" target="_blank">📺 ${v.title}（${v.channel}）</a>`
      ).join('');
    ytContainer.classList.remove('hidden');
  } else {
    ytContainer.classList.add('hidden');
  }

  // Memo
  document.getElementById('memo-text').value =
    localStorage.getItem(`memo_${q.id}`) || '';

  // Show result
  document.getElementById('answer-area').classList.add('hidden');
  document.getElementById('result-section').classList.remove('hidden');
}

function saveMemo() {
  const q = sessionQuestions[currentIndex];
  const memo = document.getElementById('memo-text').value;
  if (memo.trim()) {
    localStorage.setItem(`memo_${q.id}`, memo);
  } else {
    localStorage.removeItem(`memo_${q.id}`);
  }
}

function nextQuestion() {
  saveMemo();
  currentIndex++;
  if (currentIndex >= sessionQuestions.length) {
    showComplete();
  } else {
    renderQuestion();
  }
}

function showComplete() {
  const total = sessionQuestions.length;
  const pct = total > 0 ? Math.round(sessionCorrect / total * 100) : 0;
  document.getElementById('final-score').textContent =
    `${sessionCorrect} / ${total}問 正解（${pct}%）`;
  showScreen('screen-complete');
}
```

- [ ] **Step 2: next-btn と memo-text のイベントリスナーを Wiring セクションに追加**

`docs/app.js` の `document.getElementById('start-btn').addEventListener` の前に追記:
```javascript
document.getElementById('next-btn').addEventListener('click', nextQuestion);
document.getElementById('memo-text').addEventListener('input', saveMemo);
```

- [ ] **Step 3: ローカルサーバーで一通り動作確認**

```bash
cd docs && python -m http.server 3000
```

以下のフローを確認:
1. 科目・テーマ・年度を選択して「学習開始」
2. 問題画像が表示される
3. ア〜オのボタンをタップして解答
4. 正誤バッジ・正解表示・検索リンクが出る
5. メモを入力して「次の問題へ」で保存される
6. 全問終了後に完了画面とスコアが表示される

- [ ] **Step 4: コミット**

```bash
git add docs/app.js
git commit -m "feat: add answer submission, result display, memo and progress"
```

---

### Task 9: GitHub Pages設定・画像コミット・デプロイ

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: .gitignore に不要ファイルを追加（画像はコミット対象）**

`.gitignore` の末尾に追記:
```
.superpowers/
__pycache__/
tests/__pycache__/
*.pyc
```

docs/images/ は **gitignoreしない**（GitHub Pages で配信するため）。

- [ ] **Step 2: 画像・データを git add してコミット**

```bash
git add docs/images/ docs/data/ docs/index.html docs/app.js docs/style.css .gitignore
git commit -m "build: add generated images and quiz data for GitHub Pages"
```

- [ ] **Step 3: GitHub Pages を有効化**

1. GitHub リポジトリページ（https://github.com/danalysis707/smec-pdf-downloader）を開く
2. **Settings** → **Pages**
3. **Source**: `Deploy from a branch`
4. **Branch**: `master` / **Folder**: `/docs`
5. **Save** をクリック

- [ ] **Step 4: git push してデプロイ確認**

```bash
git push
```

数分後に `https://danalysis707.github.io/smec-pdf-downloader/` にアクセスして動作確認:
- スマホのブラウザで開く
- 科目選択・問題表示・解答・メモが動作する
- 出典（年度・科目名・設問番号）が常時表示される

- [ ] **Step 5: 最終コミット**

```bash
git add .
git commit -m "feat: complete quiz app - deployed to GitHub Pages"
git push
```
